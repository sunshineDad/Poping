# 智能体服务平台配置文件
server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: poping-platform

  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 500MB

  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://47.99.118.208:3306/poping?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
    username: root
    password: mysql20240327
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      auto-commit: true
      idle-timeout: 30000
      pool-name: PopingHikariCP
      max-lifetime: 1800000
      connection-timeout: 30000

  # Redis配置
  redis:
    host: 47.99.118.208
    port: 6379
    password: redis123
    database: 0
    timeout: 10000ms
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

  # Jackson配置
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    default-property-inclusion: non_null

  # 邮件配置
  mail:
    host: smtp.163.com
    port: 25
    username: 15724433688@163.com
    password: NQhk9wD7JJxbsGDm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# MyBatis-Plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
    call-setters-on-nulls: true
    jdbc-type-for-null: 'null'
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath*:mapper/**/*Mapper.xml

# JWT配置
jwt:
  secret: poping-platform-jwt-secret-key-2024-very-long-secret-for-hs512-algorithm-minimum-512-bits-required
  expiration: 86400000  # 24小时
  refresh-expiration: 604800000  # 7天

# 应用自定义配置
app:
  # 文件上传配置
  upload:
    path: ./uploads/
    max-file-size: 10MB
    max-request-size: 50MB
  
  # OSS对象存储配置
  oss:
    endpoint: https://oss-cn-hangzhou.aliyuncs.com
    bucket-name: poping-oss-buckeet
    access-key-id: LTAI5tDRPTsnKpv8KVFaq1ry
    access-key-secret: ${OSS_ACCESS_KEY_SECRET:}
  
  # 外部服务配置
  external:
    aigents:
      base-url: http://localhost:8000
      timeout: 30000
    mcp:
      timeout: 10000

# 日志配置
logging:
  level:
    com.poping: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: INFO
  pattern:
    console: "%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n"
  file:
    name: logs/poping-platform.log
    max-size: 10MB
    max-history: 30

# Swagger配置
springfox:
  documentation:
    swagger-ui:
      enabled: true
    swagger:
      v2:
        path: /api-docs

---
# 开发环境配置
spring:
  profiles: dev
  datasource:
    url: jdbc:mysql://localhost:3306/poping?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: 123456

logging:
  level:
    root: INFO
    com.poping: DEBUG

---
# 测试环境配置
spring:
  profiles: test
  datasource:
    url: jdbc:mysql://localhost:3306/poping_test?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: 123456

---
# 生产环境配置
spring:
  profiles: prod
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  redis:
    host: ${REDIS_HOST}
    port: ${REDIS_PORT}
    password: ${REDIS_PASSWORD}

logging:
  level:
    root: WARN
    com.poping: INFO